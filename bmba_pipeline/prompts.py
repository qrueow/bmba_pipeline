"""Prompts for Gemini API - Mode 1 (digitization) and Mode 2 (generation)."""

# Mode 1: PDF Digitization Prompt
DIGITIZE_PROMPT = """
Вы ассистент для оцифровки задач по математике из PDF сканов сертификационных тестов BMBA.

Задача: Прочитай все задачи из предложенного изображения/PDF и преобразуй их в структурированный JSON.

ВАЖНО: Все числа и формулы должны быть точно скопированы из оригинала. Не переводи условие, 
сохраняй язык оригинала.

Для каждой задачи верни JSON со следующей структурой:
{{
  "problem_number": <номер задачи>,
  "content": "<условие задачи с точно скопированными числами и формулами>",
  "solution": "<решение/ответ если виден на скане>",
  "difficulty": "<difficulty_level>",
  "theme": "<тема задачи>"
}}

Уровни сложности: easy, medium, hard
Темы: algebra, geometry, trigonometry, calculus, statistics, logic, other

Верни массив JSON-объектов (валидный JSON, можно парсить).
Если задачи невозможно разобрать - верни пустой массив [].
"""

# Mode 2: Test Generation Prompt - Two-stage process
GENERATE_PROMPT_STAGE1 = """
Ты - опытный математик, создающий новые задачи на основе исходного материала.

На вход приходит эталонная задача:
{{
  "content": "<содержание задачи>",
  "difficulty": "<сложность>",
  "theme": "<тема>"
}}

Выполни ЭТАП 1 - Абстракция:
Определи базовую структуру и схему задачи:
1. Какой математический концепт лежит в основе?
2. Какой тип вопроса (вычисление, доказательство, выбор ответа)?
3. Какие переменные можно менять (числа, вспомогательные условия)?
4. Какая минимальная математическая глубина требуется?

Верни JSON со следующей структурой (ОСТАЁТСЯ ВНУТРЕННЕЙ, на выходе не нужен):
{{
  "core_concept": "<основной концепт>",
  "problem_type": "<тип задачи>",
  "variable_parameters": [<список параметров, которые можно менять>],
  "mathematical_depth": "<уровень>",
  "original_difficulty": "<сложность>"
}}

Это описание остаётся только для твоего внутреннего анализа.
"""

GENERATE_PROMPT_STAGE2 = """
Продолжи. ЭТАП 2 - Инстанциация с трансформацией контекста.

Используя структуру из ЭТАПА 1, создай НОВУЮ задачу, которая:
1. Сохраняет тот же математический концепт
2. МЕНЯЕТ контекст/сценарий (не просто подставляет новые числа, а переформулирует ситуацию)
3. ИЗМЕНЯЕТ числовые значения случайным образом (но в разумном диапазоне)
4. Сохраняет сложность и тему оригинала
5. Содержит чек-лист "anti-copy" - явные отличия от оригинала, видимые при сравнении

Верни финальный JSON:
{{
  "content": "<НОВОЕ содержание задачи в том же языке>",
  "difficulty": "<сложность (из оригинала)>",
  "theme": "<тема (из оригинала)>",
  "context_change": "<описание изменённого контекста>",
  "numerical_changes": "<описание того, как изменены числа>",
  "anti_copy_checklist": [
    "<явное отличие 1>",
    "<явное отличие 2>",
    "<явное отличие 3>"
  ]
}}

КРИТИЧЕСКИ ВАЖНО: 
- Все числа должны быть реалистичны и математически корректны
- Изменения контекста должны быть естественными, не абсурдными
- Новая задача должна требовать ровно такого же уровня математических знаний, что и оригинал
"""

# Fallback prompts for simpler API usage
SIMPLE_DIGITIZE = """
Extract all math problems from this image. For each problem, provide:
- problem_number
- content (exact text)
- difficulty (easy/medium/hard)
- theme (algebra/geometry/etc)

Return as JSON array.
"""

SIMPLE_GENERATE = """
Create a new math problem based on this template:
{template}

Preserve the same:
- Mathematical concept
- Difficulty level
- Theme
- Language

Change:
- Numbers (different values, same difficulty)
- Context/scenario

Return as JSON with: content, difficulty, theme.
"""
