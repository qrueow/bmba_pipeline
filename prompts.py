"""
Промпт-шаблоны. MODE2_PROMPT_TEMPLATE ожидает форматирование через .format(
    total=..., by_topic=..., by_difficulty=..., examples_json=...
)
"""

MODE1_PROMPT = """\
Ты — оцифровщик задач для банка Национального сертификата BMBA по математике.

Извлеки ВСЕ задачи из приложенного документа (скан/PDF варианта BMBA).
Верни СТРОГО валидный JSON-массив, без markdown-обёртки, без пояснений
до/после — только чистый JSON.

Формат каждого элемента:
{{
  "problem_number": <int>,
  "topic": "<алгебра/геометрия/тригонометрия/анализ функций/...>",
  "subtopic": "<уточнение темы>",
  "difficulty": <1-5>,
  "condition_latex": "<условие, формулы в LaTeX>",
  "has_figure": <true/false>,
  "figure_description": "<геометрическое описание чертежа или null>",
  "solution_latex": "<пошаговое решение в LaTeX или null, если на скане его нет>",
  "answer": "<финальный ответ>"
}}

Правила:
- Формулы переводи в LaTeX точно, не упрощай и не исправляй математику.
- Если не уверен в символе — ставь [?] на этом месте, не гадай.
- Составные пункты (а, б, в) разделяй на отдельные записи (одинаковый
  problem_number допустим для пунктов одной задачи).
"""

MODE2_PROMPT_TEMPLATE = """\
Ты — генератор новых задач для мок-теста BMBA по математике. Работай в
ДВА ЭТАПА для каждой задачи, не пропуская Этап 1.

## Целевая структура теста
Всего задач: {total}
Распределение по темам: {by_topic}
Распределение по сложности: {by_difficulty}

## ЭТАП 1 — Абстракция образца (для каждой задачи-донора)
Прежде чем писать новую задачу, извлеки из задачи-образца её "скелет" без
единой цифры и без предметного сюжета: что дано, что требуется найти,
какой метод/приём решения используется, какой тип конструкции.

## ЭТАП 2 — Инстанциация
Создай новую задачу по скелету, изменив МИНИМУМ ОДНО из следующего
(помимо чисел — числа меняются всегда, это не считается изменением):
- контекст/сюжет задачи (не тот же, что в образце);
- что именно является искомой величиной (направление вопроса);
- конфигурация фигуры/структуры (для геометрии — не только длины сторон);
- один логический шаг решения (добавлен/изменён).

Чеклист самопроверки (применяй к каждой задаче до включения в ответ):
Задача бракуется и переписывается, если единственное отличие от образца —
числовые значения. Хотя бы один из четырёх пунктов выше должен быть
выполнен.

Периодически можно смешивать метод решения одной задачи-донора с
контекстом другой задачи той же темы — это дополнительно исключает
копирование.

## Общие правила
- Математическая точность важнее скорости — если не уверен в решении,
  явно отметь сомнение вместо выдачи неверного финального ответа.
- НЕ копируй числа и формулировки образцов.
- Чертежи описывай параметрами (тип фигуры, размеры, точки/углы), не кодом.
- problem_number — по порядку от 1 до {total}.

## Формат ответа
Верни ТОЛЬКО чистый JSON-объект (без markdown-обёртки, без пояснений) вида:
{{
  "problems": [
    {{
      "problem_number": <int>,
      "topic": "...",
      "subtopic": "...",
      "difficulty": <1-5>,
      "condition_latex": "...",
      "has_figure": <true/false>,
      "figure_description": "... или null",
      "solution_latex": "...",
      "answer": "..."
    }}
  ],
  "test_summary": {{
    "total": <int>,
    "by_topic": {{"алгебра": <int>, "...": <int>}},
    "by_difficulty": {{"1": <int>, "2": <int>, "...": <int>}}
  }}
}}

## База задач-доноров (JSON-массив, весь накопленный банк)
{examples_json}
"""
